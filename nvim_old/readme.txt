You have to install nodejs>=13, pynvim with pip.
link init.vim and coc-config.json to ~/.config/nvim/ (for unix system), 
                                    %userprofile%\appdata\local\nvim (for windows)

