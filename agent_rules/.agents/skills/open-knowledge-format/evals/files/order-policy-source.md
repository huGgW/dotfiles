# Order Completion Policy

Source URL: https://docs.example.com/order-completion-policy

An order is complete only when `status` is `delivered`. The `net_amount`
field is the recognized order amount after discounts. This source provides no
author, usage, verification, freshness, or lifecycle metadata.
